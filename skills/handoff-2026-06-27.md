# 📌 HANDOFF — 2026-06-27 (Billing MP F0–F4 + Exit-cortesia + Triagem em produção)

> Onde paramos hoje. Tudo abaixo foi **verificado em produção** (não é memória/palpite).

## TL;DR
Subiu pra produção, num único deploy, a **migração Mercado Pago F0–F4** (PreApprovalPlan → PreApproval avulsa) + a **F3 exit-cortesia** + a **integração Triagem** (da outra IA). **A cobrança está toda INERTE** (flag `MP_USE_PREAPPROVAL` OFF) — nada mudou para a Flávia. O botão "Triagem" no admin agora abre um app funcional. A proteção da F3 está ativa.

## Estado de produção (verificado hoje)
- **Commits:** `d955678` (código) + `528c436` (docs/skills). **prod HEAD = `528c436`**. pm2 `cliquezoom-saas` online (cluster, workers 2 e 3).
- **`MP_USE_PREAPPROVAL`: OFF** (ausente no `.env`) → checkout usa o caminho **legado** (PreApprovalPlan); os webhooks novos (F4) não disparam; tudo da migração fica inerte. **Estado seguro.**
- **Robô F3 `graceChecker`: LIVE** — `SchedulerRun.graceChecker = ok (x2)`. Roda 1×/dia; **inerte** porque nenhuma org tem `graceUntil` definido.
- **Proteção F3 ATIVA:** `.env` da VPS tem `PROTECTED_ORG_SLUGS=fsfotografias,daviconecta,rhynoproject,cliquezoom-admin` + `OWNER_SLUG=pacheco`. Essas orgs **nunca** são suspensas automaticamente.
- **`STORAGE_GATE_ENFORCE=true` em prod** — gate de storage **ENFORCANDO**. **Intencional**: ligado em 2026-06-26 com delegação do dono (a memória *detalhada* já registra; só o índice estava desatualizado). Usuários reais (Davi/Flávia) **blindados por override** → não barram; só a fixture `rhynoproject` (15MB, ~93%) barra upload >1MB (esperado). Rollback = remover a linha do `.env` + reload.
- **Triagem:** `rsync` de 183M → `/var/www/cliquezoom-triagem`; servido pela rota estática `/triagem` do app. `/triagem/` → 200, `/triagem/models/det.onnx` → 200, paths relativos OK. Botão "Triagem" no header do admin funciona.

## O que cada peça faz (resumo)
- **F0** — webhook endurecido: normaliza status MP→enum, idempotência (`lastEventId`).
- **F1** — checkout cria `PreApproval` avulsa por org (valor mutável, `payer_email` do dono), atrás da flag. Dedupe cancela a anterior (armadilha documentada: troca de plano numa sub viva deve ser `update`, não cancel+create).
- **F2** — `syncPreapprovalAmount(sub)` reflete preço custom / storage-addon na assinatura **já viva** (guards: cortesia → flag → ativo). **Provado em sandbox** que o MP honra o `update(amount)`.
- **F3 (exit-cortesia)** — carência por org (`Subscription.graceUntil`); vencido sem regularizar → **suspende** (`Organization.suspendedReason='billing'`, `isActive=false`) **sem excluir** (offboarding blindado contra `suspendedReason='billing'`); contas protegidas via env; reativação manual (`/approve`); UI: date-picker + banner "suspensa por cobrança" + botão Reativar no painel super-admin; confirmação reforçada nas protegidas.
- **F4** — faturas recorrentes (webhook `subscription_authorized_payment`) → `active` (pago) ou `past_due` (inadimplência); guard de cortesia **atômico**; `mpPreapprovalId` `unique+sparse`; status da assinatura + última fatura visíveis no painel.
- **Triagem** — `/api/triagem/heartbeat` (licença, auth+rate-limit) + serve `/triagem` estático + botão no admin.

## Para LIGAR a cobrança de verdade (quando o dono quiser)
1. **5 decisões abertas:** (a) fluxo de cartão init_point hospedado [recomendado] vs Bricks; (b) confirmar a conta MP definitiva; (c) HMAC no webhook (lançamento ou pós); (d) confirmar sem pró-rata; (e) confirmar que Flávia/Davi/Rhyno nunca saem da cortesia sozinhos.
2. Setar `MP_USE_PREAPPROVAL=true` no `.env` da VPS + reload.
3. **1ª fatura real** cobrando o valor novo = a única prova que falta (sandbox não gera fatura automática).

## Pendências (para amanhã)
1. **Validar Triagem no browser** (lane front/Gemini) — serving OK; falta rodar a separação facial ONNX 1× logado.
2. **Bug pré-existente:** agente IA do SaaS Admin configurado com modelo inexistente `'GPT-5.4 mini'` → chamadas de IA falham. Trocar o nome do modelo na config.
3. **Storage gate** (`STORAGE_GATE_ENFORCE=true`) já ativo/intencional — só **monitorar** se algum usuário real legítimo começar a barrar (hoje só a fixture `rhynoproject` barra).
4. **Dívida de deploy acumulada** (vários fixes codados em sessões passadas, não deployados — fazer varredura antes de assumir que estão no ar): cards "originais × editadas", gate de entrega, bug ZIP foto-em-edição sem marca (`sessions.js ~2172`), `pm2 reload` de auto-inscrição QR / upsell extras / cards de etapa.
5. **Verificar** se o ambiente **beta** está gravando no DB de **produção** (risco de integridade — anotado em `project_prod_erros_triagem`).
6. **Rhyno ERP** — deploy pendente do handoff 23/06 (botão "Concluir evento" + modal "Registrar contato"); ficou por timeout de SSH.
7. **CLAUDE.md** tem refs quebradas a `skills/*` após o reorg de hoje (vários movidos p/ `skills/archive/`) — limpeza de docs.
8. **Roadmap grande:** Triagem v1 E2E; **Seleção Real-Time** (B2, o flagship, não começado).

## Sugestão de retomada (amanhã)
1. **Varredura de verificação** rápida — confirmar (no código/VPS) quais pendências de memória ainda estão de fato abertas (várias podem já ter sido resolvidas).
2. Decidir: atacar o **billing** (5 decisões → ligar a flag) ou a **dívida de deploy** + o bug do beta-em-prod.
3. Commitar este handoff + a atualização do CLAUDE.md (estão como working-tree não commitado).

## Ponteiros
- Memória detalhada: `project_preapproval_migracao` (F0–F4), `project_cliquezoom_triagem` (triagem), `project_storage_motor_saturacao` (gate), `project_master_roadmap_lancamento` (roadmap A1/A2/B1/B2).
- Deploy: `cd /var/www/cz-saas && git pull && pm2 reload ecosystem.config.js --env production --update-env` (SSH `-i ~/.ssh/cz_vps root@5.189.174.18`).
